# BNFMIP ELM-FUN — Stock-take & Resume Notes (2026-07-16, pre-shutdown)

Machine shuts down 16:00 today; all job submission stopped. This file is the resume point.

## What we are doing (big picture)
Rebuilding the ELM-FUN BNFMIP **site deliverable for Tom** after the original scratch died.
Target = **36 files**: 3 sites × 4 experiments × 3 sections.
- Sites: **Manaus** (BNF-Man, PFT4 tropical), **Harvard** (BNF-Ha1, PFT7 temperate), **Bonanza** (BNF-Bon, PFT2 boreal).
- Experiments: **nofun** / **fun** (Houlton) / **noacc** (Bytnerowicz) / **acc** (Bytnerowicz-acclim).
- Sections: **§5.3** historical 1850–2014 · **§5.4** fixed CO2/Ndep 2015–2100 · **§5.5** SSP5-8.5 2015–2100.

The first-generation rebuild (10/12 combos, §5.3+§5.5) worked but showed a **pre-industrial "U-shape"**
in the carbon pools. We diagnosed and are fixing that; this second generation ("spun-up") is the real deliverable.

## DONE (verified)
1. **Root cause of the U-shape** — every prior spin-up ran `use_funp=.false.` while transients run
   `use_funp=.true.` → the P cycle started cold → pools relaxed down then recovered. `nofun` exempt.
2. **Fix validated** — CNP+FUNP spin-up (I1850, use_fun+funp) equilibrates C/veg/GPP/NPP/BNF
   (Bon-fun proof: BNF flat 2.14 kgN/ha/yr). Mineral SMINN/SMINP still slowly fill — **PI decision:
   accept C/BNF equilibrium, document mineral residual**.
3. **BNF literature benchmark** — Bonanza 2.17 = DeLuca 1.5–2.0; Harvard 12–15 = Cleveland 7–27;
   Manaus 23 = upper Cleveland 15–36. **No `s_fix` tuning.** NoFUN Bonanza (6.65) is the high one.
4. **9/9 CNP+FUNP spin-ups COMPLETE** (fun/noacc/acc × 3 sites), all at `r.0301`.
   Backed up to `/home/braghiere/BNF_tom/restart_opt/spunup_cnp/spunup_<combo>_r0301.nc` (survives scratch purge).
5. **acc-ha1 "slowness" was NOT real** — profiling (identical restart, same node pool) shows acc = noacc
   cost (LND 1.0×, CNFUN 1.0×). Code diff is trivial (a piecewise if/else + the same `**` op). The two
   failures were: my wrong-restart bug (fixed) + node `c198` hardware hang (excluded). No code fix needed.
6. **do_transient_pfts** — every site is 100% single-PFT constant (verified 3 ways); output identical to
   transient-off. §5.4 uses `do_transient_pfts=.false.` (needed for the 2015-start; flanduse kept only for build-namelist).
7. **Committed to git** (`bnfmip-submission`, commit 634850b + this file): findings log
   (`docs/REBUILD_NOTES_2026-07.md`), all `scripts/rebuild/*`, gallery notebook.

## INTERRUPTED by the shutdown
- **Re-branch of the 9 spun-up transients** — stopped. One (`su_fun_bon`) was submitted then cancelled
  (couldn't finish before 16:00). **Zero spun-up transients completed.**

## MISSING (to finish the deliverable — the resume to-do list)
1. **Re-branch 9 transients** from the spun-up `r.0301` → spun-up §5.3+§5.5 (no U-shape).
   Script ready: `/home/braghiere/rebranch.sh` (I20TR CNP+FUNP, `do_transient_pfts=.false.`,
   `REST_N=5` so `r.2015` is saved, bad nodes c198/c46 excluded). **Just re-run it when the machine is back.**
2. **Build Man-nofun + Ha1-nofun** — these 2 combos never existed (old pftdyn crash). Need §5.3+§5.5.
   (nofun = use_fun/funp OFF, control source; branch from the `fn_/fn2_nofun_*` restarts already on scratch.)
3. **§5.4 (fixed CO2/Ndep) for all 12 combos** — needs `r.2015` from step-1 transients.
   Config: `DATM_CO2_TSERIES=none`, `CCSM_CO2_PPMV=401.5`, `stream_year_first/last_ndep=2015`,
   `do_transient_pfts=.false.` (Bonanza §5.4 already validated end-to-end in gen-1).
4. **Re-package** (`scripts/rebuild/package_table2_rebuild.py`, 54-var VMAP, decimal-year time,
   strip phantom boundary records) → `delivery_rebuild/`; **re-run gallery**
   (`notebooks/BNFMIP_table2_gallery_REBUILD.ipynb`); verify flat pre-industrial pools; commit.
5. **Deliver to Tom** + put the final set somewhere durable (not scratch; `/home` is 98% full — see below).

## KEY ARTIFACTS (where to resume from)
- Spun-up restarts: `/home/braghiere/BNF_tom/restart_opt/spunup_cnp/` (9 files, durable) AND
  `/lustre/or-scratch24/.../spinCNP_<combo>_*/run/*.r.0301*` (scratch).
- Re-branch driver: `/home/braghiere/rebranch.sh` (+ copy in `scripts/rebuild/`).
- Site inputs (surfdata/params/met/domain): all on `/home/braghiere/BNF_tom/inputdata` (durable).
  Note: **Harvard met dir is `BNFMIP_forcing_from_OCN/BNF_Har`** (not BNF_Ha1); Ha1 domain staged to
  `inputdata/domain_BNF-Ha1_1x1.nc`.

## ENVIRONMENT / RISKS
- **This machine shuts down 16:00 today.** Running jobs die; nothing new submitted.
- `/lustre/or-scratch` (OLD) is read-only/decommissioned (dead — caused this rebuild). **No CLM met or
  inputs of ours live there** — verified. Runs are on `/lustre/or-scratch24` (surviving scratch, purgeable).
- Global GSWP3 met: `/nfs` (932 G, persistent) + re-derivable `or-scratch24` copy (1.2 T).
- `/home` is **98% full (15 T free)** — fine for our MB-scale artifacts, but not a long-term deliverable home.
- **ISAAC migration recommended by CADES** (2nd scratch failure) — PI decision + email reply pending (user).

## ONE-LINE RESUME
When the machine is back: `nohup bash /home/braghiere/rebranch.sh &` → wait for 9 transients →
build Man/Ha1 nofun + all §5.4 → repackage + gallery → deliver.
